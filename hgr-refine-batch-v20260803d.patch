diff --git a/index.html b/index.html
index 1992400..095eebc 100644
--- a/index.html
+++ b/index.html
@@ -154,7 +154,7 @@
     
     <!-- ============================================ -->
     <!-- CACHE-BUSTING: When updating, change v=XXXXXX in ALL script tags below to match APP_VERSION -->
-    <!-- Current version: v20260803c --> 
+    <!-- Current version: v20260803d --> 
     <!-- ============================================ -->
     
     <!-- Recognition modules -->
@@ -170,18 +170,18 @@
     <script src="src/reading/transcription.js"></script>
     
     <!-- UI Components (loaded as Babel) -->
-    <script type="text/babel" src="src/components/common/CommonComponents.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/common/DetectionCard.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/panels/InscriptionPanel.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/panels/ExportPanel.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/common/AccessControlUI.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/modals/WarehouseModal.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/modals/CollaboratorManager.jsx?v=20260803c"></script>
-    <script type="text/babel" src="src/components/modals/BookletGenerator.jsx?v=20260803c"></script>
+    <script type="text/babel" src="src/components/common/CommonComponents.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/common/DetectionCard.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/panels/InscriptionPanel.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/panels/ExportPanel.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/common/AccessControlUI.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/modals/WarehouseModal.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/modals/CollaboratorManager.jsx?v=20260803d"></script>
+    <script type="text/babel" src="src/components/modals/BookletGenerator.jsx?v=20260803d"></script>
 
     <!-- Main Application -->
     <script type="text/babel">
-        const APP_VERSION = 'v20260803c';
+        const APP_VERSION = 'v20260803d';
         console.log('🚀 Hakli Glyph Recognizer ' + APP_VERSION + ' (Transcription Syncs with Reading Order!)');
 
         function HakliGlyphRecognizer() {
@@ -3338,12 +3338,9 @@
                                         onClick={() => setShowChartManager(true)}
                                         disabled={chartStatus !== 'loaded'}
                                         className={`px-3 py-1 rounded-lg text-xs font-medium disabled:opacity-50 ${chartModified ? 'bg-ochre hover:bg-ochre/80' : 'bg-white/20 hover:bg-white/30'}`}
-                                        title={chartVersion ? `Chart version ${chartVersion}` : 'Manage glyph chart'}
+                                        title={chartVersion ? `Chart v${chartVersion} · ${chartData?.glyphs?.length || 0} glyphs — click to manage` : 'Manage glyph chart'}
                                     >
-                                        {chartModified ? '⚠️' : '📊'} Chart{chartVersion ? ` v${chartVersion}` : ''}{chartModified ? '*' : ''}
-                                        {chartStatus === 'loaded' && (
-                                            <span className="opacity-80"> · {chartData?.glyphs?.length || 0}<span className="hidden sm:inline"> glyphs</span></span>
-                                        )}
+                                        {chartModified ? '⚠️' : '📊'} Chart{chartModified ? '*' : ''}
                                     </button>
                                     
                                     {/* Status indicators - grouped on right */}
@@ -5741,7 +5738,7 @@
                                     <div>
                                         <h3 className="text-xl font-bold text-gray-900">📊 Glyph Chart Manager</h3>
                                         <p className="text-sm text-gray-500">
-                                            {chartData?.glyphs?.length || 0} glyphs · {(() => {
+                                            {chartVersion ? `v${chartVersion} · ` : ''}{chartData?.glyphs?.length || 0} glyphs · {(() => {
                                                 const total = chartData?.glyphs?.reduce((sum, g) => {
                                                     return sum + (g.images?.primary ? 1 : 0) + (g.images?.variants?.length || 0) + (g.images?.examples?.length || 0);
                                                 }, 0) || 0;
diff --git a/src/components/common/DetectionCard.jsx b/src/components/common/DetectionCard.jsx
index fae88bd..867fd52 100644
--- a/src/components/common/DetectionCard.jsx
+++ b/src/components/common/DetectionCard.jsx
@@ -139,6 +139,10 @@ const DetectionCard = ({
                 className={`w-full px-3 ${isMobile ? 'py-2.5' : 'py-1.5'} text-left text-xs hover:bg-gray-100 flex items-center gap-2 ${isExcluded ? 'text-patina' : 'text-rust'}`}>
                 {isExcluded ? '👁 Include' : '🚫 Exclude'}
             </button>
+            <button onClick={(e) => { e.stopPropagation(); if (confirm('Delete this detection?')) { deleteDetection(idx); setEditMenuCardIdx(null); } }}
+                className={`w-full px-3 ${isMobile ? 'py-2.5' : 'py-1.5'} text-left text-xs hover:bg-gray-100 flex items-center gap-2 text-red-600`}>
+                🗑 Delete detection
+            </button>
             {historyEnabled && (
                 <>
                     <div className="border-t border-gray-100 my-1"></div>
@@ -193,9 +197,7 @@ const DetectionCard = ({
                                 <div className={`${isMobile ? 'w-12 h-12' : 'w-10 h-10'} bg-gray-100 rounded overflow-hidden border-2 transition-colors ${result.thumbnailEdited ? 'border-patina' : 'border-gray-300'} group-hover:border-blue-400`}>
                                     <img src={result.thumbnail} alt="Detected" className="w-full h-full object-contain" />
                                 </div>
-                                <span className={`absolute -top-1 -left-1 bg-white rounded-full w-4 h-4 flex items-center justify-center text-[9px] shadow-sm border opacity-80 group-hover:opacity-100 ${result.thumbnailEdited ? 'border-patina text-patina' : 'border-gray-200 group-hover:text-blue-500'}`}>
-                                    {result.thumbnailEdited ? '✓' : '✏️'}
-                                </span>
+                                <span className={`absolute -bottom-1 left-1 right-1 h-0.5 rounded-full ${result.thumbnailEdited ? 'bg-patina' : 'bg-blue-400/70 group-hover:bg-blue-500'}`}></span>
                             </div>
                         )}
                         {glyphThumbnails[result.glyph.id] && (
@@ -206,7 +208,7 @@ const DetectionCard = ({
                                 <div className={`${isMobile ? 'w-12 h-12' : 'w-10 h-10'} bg-white rounded overflow-hidden border-2 border-ancient-purple transition-colors group-hover:border-ochre`}>
                                     <img src={glyphThumbnails[result.glyph.id]} alt="Chart" className="w-full h-full object-contain" />
                                 </div>
-                                <span className="absolute -top-1 -right-1 bg-white rounded-full w-4 h-4 flex items-center justify-center text-[9px] shadow-sm border border-ancient-purple/40 text-ancient-purple opacity-80 group-hover:opacity-100 group-hover:text-ochre">⇄</span>
+                                <span className="absolute -bottom-1 left-1 right-1 h-0.5 rounded-full bg-ancient-purple/60 group-hover:bg-ochre"></span>
                             </div>
                         )}
                     </div>
@@ -235,16 +237,14 @@ const DetectionCard = ({
                         {!validation ? (
                             <>
                                 <button onClick={(e) => { e.stopPropagation(); validateDetection(idx, true); }}
-                                    className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-patina text-white rounded text-xs hover:bg-[#5a7d6e]`} title="Mark correct">✓</button>
+                                    className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-patina text-white rounded text-xs hover:bg-[#5a7d6e]`} title="Confirm — mark reviewed">✓</button>
                                 <button onClick={(e) => { e.stopPropagation(); validateDetection(idx, false); }}
-                                    className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-rust text-white rounded text-xs hover:bg-[#8a574a]`} title="Mark incorrect">✗</button>
+                                    className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-rust text-white rounded text-xs hover:bg-[#8a574a]`} title="Flag for review">✗</button>
                             </>
                         ) : (
                             <button onClick={(e) => { e.stopPropagation(); setValidations(prev => { const n = { ...prev }; delete n[idx]; return n; }); }}
-                                className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-gray-400 text-white rounded text-xs hover:bg-gray-500`} title="Clear validation">↩</button>
+                                className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-gray-400 text-white rounded text-xs hover:bg-gray-500`} title="Clear review status">↩</button>
                         )}
-                        <button onClick={(e) => { e.stopPropagation(); if (confirm('Delete?')) deleteDetection(idx); }}
-                            className={`${isMobile ? 'w-7 h-7' : 'w-6 h-6'} bg-red-500 text-white rounded text-xs hover:bg-red-600`} title="Delete">🗑</button>
                     </div>
                 </div>
             </div>
diff --git a/sw.js b/sw.js
index b3fd933..03ac772 100644
--- a/sw.js
+++ b/sw.js
@@ -1,7 +1,7 @@
 // Hakli Glyph Recognizer - Service Worker
-// Version 20260803c - Network First for same-origin app code (no more stale .js)
+// Version 20260803d - Network First for same-origin app code (no more stale .js)
 
-const CACHE_VERSION = 'v20260803c';
+const CACHE_VERSION = 'v20260803d';
 const CACHE_NAME = `hakli-${CACHE_VERSION}`;
 const RUNTIME_CACHE = `hakli-runtime-${CACHE_VERSION}`;
 
